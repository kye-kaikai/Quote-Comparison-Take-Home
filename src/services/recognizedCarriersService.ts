export class RecognizedCarriers {
    static readonly carrierToFilePaths: Map<string, string> = new Map([
        ['carrier-a', './data/carrier-a-quote.csv'],
        ['carrier-b', './data/carrier-b-quote.json'],
        ['carrier-c', './data/carrier-c-quote.csv'],
    ])
}